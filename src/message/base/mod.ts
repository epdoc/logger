import { AbstractMsgBuilder } from './builder.ts';
import type { IBuilder } from './types.ts';

// Re-export the class under the desired name 'Builder'
export { AbstractMsgBuilder as Builder };
export type { IBuilder };
